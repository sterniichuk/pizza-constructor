import React, {forwardRef, useRef} from 'react';
import Header from './Header';
import MainBody from './MainBody';

function App() {
    return (
        <>
            <Header></Header>
            <MainBody/>
        </>
    );
}

export default App;
